# DocAuditor - exact versions verified working on Python 3.12 / CUDA 12.1.
#
# torch is installed from the CUDA 12.1 index; on a CPU-only machine install
# plain `torch` instead and everything still runs, just slower:
#
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
#
# transformers 5.x removed the "question-answering" pipeline task, so the QA
# head is loaded directly and start/end logits are decoded by hand. Pinning
# matters here: older code that calls pipeline("question-answering") will not
# run on this version.

torch==2.5.1+cu121
transformers==5.14.1
sentence-transformers==5.6.0
accelerate==1.14.0

streamlit==1.59.2
pandas==3.0.3
numpy==2.5.1
scikit-learn==1.9.0

# Document input: PDF and DOCX extraction
pymupdf==1.28.0
python-docx==1.2.0

# Regenerating the architecture PDF (scripts/build_architecture_pdf.py)
reportlab==5.0.0

# Running the notebook
jupyter==1.1.1
ipykernel==7.3.0
